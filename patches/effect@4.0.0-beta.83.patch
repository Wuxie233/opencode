diff --git a/dist/unstable/httpapi/HttpApiSchema.js b/dist/unstable/httpapi/HttpApiSchema.js
index ecb6603d1ee4e89e92174b3a1100e8c9c720b3f3..08b832b6b3e9e4d10df156eca36b16f571f67f6e 100644
--- a/dist/unstable/httpapi/HttpApiSchema.js
+++ b/dist/unstable/httpapi/HttpApiSchema.js
@@ -151,7 +151,7 @@ export const StreamSse = options => {
   const events = options.events ?? (options.data === undefined ? undefined : Schema.Struct({
     id: Schema.UndefinedOr(Schema.String),
     event: Schema.String,
-    data: Schema.fromJsonString(options.data)
+    data: sseDataJsonSchema(options.data)
   }));
   if (events === undefined) {
     throw new Error("StreamSse requires either an events schema or a data schema");
@@ -166,6 +166,14 @@ export const StreamSse = options => {
     error: options.error ?? Schema.Never
   });
 };
+const sseDataJsonSchema = data => {
+  const identifier = SchemaAST.resolveIdentifier(data.ast);
+  return identifier === undefined ? Schema.fromJsonString(data) : Schema.fromJsonString(data).annotate({
+    // The SSE transport field is a JSON string. Give that wrapper its own
+    // OpenAPI identifier so it does not claim the decoded data schema's name.
+    identifier: `${identifier}Stream`
+  });
+};
 /**
  * Creates a streaming `Uint8Array` success response schema.
  *
diff --git a/src/unstable/httpapi/HttpApiSchema.ts b/src/unstable/httpapi/HttpApiSchema.ts
index f2920365cf0328146523ce2e2dba04d6a8809a4a..5482d635cdf01a72063ff683d244fab891910211 100644
--- a/src/unstable/httpapi/HttpApiSchema.ts
+++ b/src/unstable/httpapi/HttpApiSchema.ts
@@ -433,7 +433,7 @@ export const StreamSse: {
   const events = options.events ?? (options.data === undefined ? undefined : Schema.Struct({
     id: Schema.UndefinedOr(Schema.String),
     event: Schema.String,
-    data: Schema.fromJsonString(options.data)
+    data: sseDataJsonSchema(options.data)
   }))
   if (events === undefined) {
     throw new Error("StreamSse requires either an events schema or a data schema")
@@ -449,6 +449,15 @@ export const StreamSse: {
   })
 }
 
+const sseDataJsonSchema = (data: Schema.Constraint) => {
+  const identifier = SchemaAST.resolveIdentifier(data.ast)
+  return identifier === undefined ? Schema.fromJsonString(data) : Schema.fromJsonString(data).annotate({
+    // The SSE transport field is a JSON string. Give that wrapper its own
+    // OpenAPI identifier so it does not claim the decoded data schema's name.
+    identifier: `${identifier}Stream`
+  })
+}
+
 /**
  * Creates a streaming `Uint8Array` success response schema.
  *
diff --git a/dist/unstable/sql/SqlClient.js b/dist/unstable/sql/SqlClient.js
index 75d651b..7d88f2c 100644
--- a/dist/unstable/sql/SqlClient.js
+++ b/dist/unstable/sql/SqlClient.js
@@ -124,7 +124,9 @@ export const makeWithTransaction = options => effect => {
       }
       const withScope = scope !== undefined ? Effect.ensuring(effect, Scope.close(scope, exit)) : effect;
       return Effect.flatMap(withScope, () => exit);
-    })));
+    }))).pipe(Effect.withParentSpan(span, {
+      captureStackTrace: false
+    }));
   })));
 };
 /**
diff --git a/src/unstable/sql/SqlClient.ts b/src/unstable/sql/SqlClient.ts
index 53bcf6b..4b3e4b5 100644
--- a/src/unstable/sql/SqlClient.ts
+++ b/src/unstable/sql/SqlClient.ts
@@ -285,7 +285,7 @@ export const makeWithTransaction = <I, S>(options: {
                   return Effect.flatMap(withScope, () => exit)
                 })
               )
-          )
+          ).pipe(Effect.withParentSpan(span, { captureStackTrace: false }))
         })
     )
   )
diff --git a/dist/unstable/sql/Statement.js b/dist/unstable/sql/Statement.js
index 11b3adf..bc41841 100644
--- a/dist/unstable/sql/Statement.js
+++ b/dist/unstable/sql/Statement.js
@@ -666,7 +666,9 @@ const StatementProto = {
       }
       span.attribute(ATTR_DB_OPERATION_NAME, operation);
       span.attribute(ATTR_DB_QUERY_TEXT, sql);
-      return Effect.scoped(Effect.flatMap(this.acquirer, _ => f(_, sql, params)));
+      return Effect.scoped(Effect.flatMap(this.acquirer, _ => f(_, sql, params))).pipe(Effect.withParentSpan(span, {
+        captureStackTrace: false
+      }));
     });
   },
   get withoutTransform() {
diff --git a/src/unstable/sql/Statement.ts b/src/unstable/sql/Statement.ts
index 85de0ab..16e7e3f 100644
--- a/src/unstable/sql/Statement.ts
+++ b/src/unstable/sql/Statement.ts
@@ -1324,6 +1324,8 @@ const StatementProto: StatementProto = {
       }
       span.attribute(ATTR_DB_OPERATION_NAME, operation)
       span.attribute(ATTR_DB_QUERY_TEXT, sql)
-      return Effect.scoped(Effect.flatMap(this.acquirer, (_) => f(_, sql, params)))
+      return Effect.scoped(Effect.flatMap(this.acquirer, (_) => f(_, sql, params))).pipe(
+        Effect.withParentSpan(span, { captureStackTrace: false })
+      )
     })
   },
